---
import { MenuData } from "../data/MenuData"
---

            <nav class="flex space-x-5">
                {MenuData.map((item)=>(
                        <a href={item.href} class="text-white text-2xl">{item.texto}</a>
                    )
                )}
            </nav>